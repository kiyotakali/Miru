[pytest]
testpaths = tests
python_files = test_*.py
markers =
    integration: spawns subprocesses, hits real services; not part of default unit run
    slow: takes > 5s (e.g. real LLM round-trips)
addopts = -m "not integration"
